import React, { FC, memo } from 'react';

export const ChangePasswordComponent: FC<{}> = () => {
  return <div>ChangePasswordComponent // use updateUserData endpoint</div>;
};

export default memo(ChangePasswordComponent);
